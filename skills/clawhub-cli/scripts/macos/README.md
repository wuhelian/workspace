# macOS Notes

The scripts in `scripts/linux/` are written as portable `bash` scripts and typically work on macOS as well.

If your environment defaults to `zsh`, you can still run them with:

```bash
bash scripts/linux/install-skill.sh <skill-slug>
```

If you hit permission issues, make them executable:

```bash
chmod +x scripts/linux/*.sh
```

