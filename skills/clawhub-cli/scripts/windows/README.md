# Windows (No Scripts)

This skill package does **not** include `.ps1` scripts.

Use `clawhub` directly from PowerShell / cmd, or run the `.sh` scripts via Git Bash or WSL.

See: `reference/WINDOWS-USAGE.md`

